import React, { ReactNode } from "react";
export const metadata = {
  title: "Login Page",
  description: "Pack Vack Login Page",
};
export default function layout({ children }) {
  return <>{children}</>;
}
